#include <bits/stdc++.h>
using namespace std;

int main()
{
    int x, n;
    char s[100];
    cin.getline(s, 23, 'h');
    //         char arr in which  | length of | delimiter
    //         string is stored,     string ,
    puts(s);

    return 0;
}